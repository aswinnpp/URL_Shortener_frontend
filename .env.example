# Use /api in development (Vite proxy forwards to backend and attaches Bearer from cookies)
VITE_API_BASE_URL=/api

# For production with a reverse proxy on the same origin, keep /api
# For direct backend access in production, set full URL — ensure your proxy adds Authorization from cookies
# VITE_API_BASE_URL=https://your-domain.com/api
